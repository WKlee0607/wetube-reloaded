extends ../base.pug
include ../mixins/subscriptionVideos

block content
    div.subscription
        each video in descVideos 
            +subVideos(video)
        else
            span.empty__message Channels that you subscribed did not upload videos yet or Subscribe channels

