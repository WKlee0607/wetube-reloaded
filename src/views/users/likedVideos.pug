extends ../base
include ../mixins/subscriptionVideos

block content
    div 
        h1=pageTitle
    each video in likedVideos
            +subVideos(video)
    else
        span.empty__message You didn't like videos yet
